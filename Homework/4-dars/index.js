let a = +prompt('a?', '');

switch (a) { 
    case (a == 0) :
    console.log(0);
    break;
    case (a == 1) :
    console.log(1);
    break;
    case (a == 2 || a == 3) :
        console.log('2,3');
}

let age = prompt(brower); 
    if (Edge) {
        console.log("You've got the Edge");
    }
    else if (CHrome) {}
    
    else if (Fifefox) {}

    else if (Safari) {}

    else if (Opera) {
    console.log('Okay we support these browsers too');
    }
    else {
        console.log('We hope that this page looks ak !');
    }

    let message = 
  login == 'Employee' ? 'Hello' :
  login == 'Director' ? 'Greetings' :
  login == '' ? 'No login' :
  '';

  let result = (a + b < 4) ? 'Below' : 'Over';
